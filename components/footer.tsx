import Image from "next/image";

export function Footer() {
  return (
    <footer className="flex flex-col gap-0 items-center justify-center py-1 mt-auto">
      <div className="flex items-center gap-0">
        <span className="text-[15px] font-semibold uppercase tracking-[0.5em] text-slate-500">
          Finyx WaaS Studio
        </span>
      </div>
    </footer>
  );
}
