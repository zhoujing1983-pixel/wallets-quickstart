/*
 * 文件作用：核心模块出口：聚合 core 能力并向外暴露稳定导入入口。
 * 调用链阶段：通用支撑阶段
 * 调用链关系：上游：业务模块通过 src/agent/core/index.ts::(模块导入入口) 引用；下游：src/agent/core/index.ts::(统一导出入口) 把 core 子模块能力暴露给调用方。
 * 维护说明：新增/修改本文件时，应保持输入输出契约稳定，避免破坏上游调用方与下游被调方的方法签名。
 */
/*
 * 核心模块的统一出口占位：
 * - 用于集中导出 core 下的公共能力与类型；
 * - 便于上层以单一入口引用，避免深层路径耦合；
 * - 当前无显式导出，后续按需补齐。
 */
