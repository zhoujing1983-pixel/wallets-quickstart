/*
 * 文件作用：Agent 应用入口占位：预留服务启动与初始化挂载点，说明当前主引擎位置。
 * 调用链阶段：启动与导出阶段（包入口）
 * 调用链关系：上游：应用启动脚本调用 src/agent/app.ts::(入口挂载位)；下游：src/agent/engine/voltagent-engine.ts::agent/workflows（当前实际执行实现）。
 * 维护说明：新增/修改本文件时，应保持输入输出契约稳定，避免破坏上游调用方与下游被调方的方法签名。
 */
/*
 * Agent 应用入口占位文件：
 * - 预留给 CLI / 服务端的启动与初始化逻辑；
 * - 目前核心引擎在 voltagent-engine.ts 中维护；
 * - 保留目录结构，便于后续拆分与扩展。
 */
